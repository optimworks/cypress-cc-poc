export class loginPageLocators {
    static SignLink = '.panel .authorization-link';
    static verifyTitle = '.base';
    static username =  '#email';
    static password = '[name="login[password]"]';
    static verifySignin ='.action.login span';
    static signin = '.action.login'
}